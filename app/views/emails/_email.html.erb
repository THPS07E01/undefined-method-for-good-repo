<div id="email">
  <h3><%= @email.object %></h3>
  <p><%= @email.body %></p>
  <%= link_to @email, method: :delete, data: { confirm: 'Are you sure?' }, remote: true, class: 'delete' do %>
  <i class="fas fa-trash-alt text-danger"></i>
  <% end %>
</div>
